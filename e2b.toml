# E2B Worker Configuration
# Run: e2b build && e2b deploy

[build]
entry_point = "src/services/e2bWorker.ts"
build_cmd = "npm install && npx tsc src/services/e2bWorker.ts --outDir dist --module commonjs --target es2020"

[runtime]
node_version = "18"

[env]
# E2B automatically handles API key injection
