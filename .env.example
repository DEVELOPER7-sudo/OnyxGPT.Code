# Environment variables for OnyxGPT
# Copy this file to .env.local and fill in your values

# Frontend-only deployment configuration
# No backend server required - all processing happens in the browser

# Vercel deployment URL (optional, for reference)
# VITE_API_ENDPOINT=https://onyx-gpt-code.vercel.app

# Note: 
# - Gemini API key is set through the UI (not in env variables for security)
# - All project data stored in browser localStorage
# - This is a frontend-only application
